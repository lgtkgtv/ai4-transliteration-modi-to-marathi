# Examples

Sample Modi image → expected Marathi (Devanagari) output.

<!-- TODO: add a representative sample image and its verified Devanagari transliteration -->

Example invocation (once `src/` code is ready):

```bash
python src/transliterate.py --image examples/sample.jpg
# Expected output (Devanagari): ...
```

The model produces a first draft. A human expert should verify and correct the output — this is an assistive tool, not an autonomous reader.
